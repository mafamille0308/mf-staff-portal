import { render } from "../ui.js";

export function renderCustomersPlaceholder(appEl) {
  render(appEl, `
    <section class="section">
      <h1 class="h1">担当顧客一覧</h1>
      <p class="p">次段で実装します。</p>
    </section>
  `);
}
